pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Payelsinharoy/my-first-pipeline.git'
            }
        }
        stage('Build') {
            steps {
                echo 'Hello, Jenkins!'
            }
        }
    }
}
